import { settings as userSettings } from '../slider-settings';

const sliderName = 'SlideeSlider';
const sliderStorage = {
  sliderContainer: null,
  controlsContainer: null,
  slides: null,
  slideStyles: null,
};
